import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jotsauce',
  appName: 'jotsauce',
  webDir: '../jotsauce/public/scovue',
  bundledWebRuntime: false,
};

export default config;
